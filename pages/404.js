import Head from 'next/head'
import Link from 'next/link'
import { Flex, Text, Button } from "@chakra-ui/react";
import color from '@/lib/color';

const NotFound = () => {
    return (
        <div className="not-found">
            <Head>
                <title>Page Not Found</title>
                <meta name="description" content="Generated by create next app" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <Flex flexDirection="column" justifyContent="center" alignItems="center" height='90vh'>
                <Text fontSize={{base: "8xl"}} className="not-found-text" color={color['primary']}>404</Text>
                <Text fontSize={{base: "4xl", md: "6xl"}} className="not-found-text" color={color['primary']}>Page Not Found</Text>
                <Button mt={6} size="lg" borderColor={color['primary']} color={color['primary']} variant="outline"><Link style={{ textDecoration: 'none' }} href="/">Back to Home</Link></Button>
            </Flex>
        </div>
    );
}
 
export default NotFound;